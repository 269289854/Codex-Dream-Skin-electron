# Codex Dream Skin Studio v1.1.22 - New Conversation Navigation Alignment

- Aligned the Chinese primary navigation label with current Codex: the first item is now “新对话”.
- Kept “新建任务”, “New task”, and “New chat” as runtime aliases so older Codex DOM variants remain injectable.
- Migrated old default navigation copy in existing themes while preserving custom labels.
- Added current home DOM regression coverage for navigation detection and composer-surface mounting.
- Fixed large Polaroid image data URLs by using revocable Blob URLs in runtime CSS.

The Windows package is an x64 NSIS installer. The macOS packages are Universal DMG and ZIP builds and remain unsigned and unnotarized. The Windows installer includes its matching blockmap and `latest.yml` for the updater.
